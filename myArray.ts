const superHeros: string[] = [];
const heroPower: number[] = [];
const heroNumber: Array<number> = [];

superHeros.push("Spiderman");
heroPower.push(3);
heroNumber.push(1);

type User = {
  name: string;
  isActive: boolean;
};

const allUsers: User[] = [];

allUsers.push({ name: "Spiderman", isActive: true });
